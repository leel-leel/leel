<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Seller Dashboard — LEEL</title>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;600&family=Noto+Sans+KR:wght@300;400;500&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --black: #0a0a0a; --white: #ffffff;
      --gray-light: #f5f5f5; --gray-mid: #e0e0e0;
      --gray-dark: #888888; --gold: #c9a96e;
      --font-en: 'Cormorant Garamond', serif;
      --font-kr: 'Noto Sans KR', sans-serif;
    }
    body { font-family: var(--font-kr); background: var(--gray-light); color: var(--black); }

    /* SIDEBAR */
    .dashboard-wrap { display: grid; grid-template-columns: 240px 1fr; min-height: 100vh; }

    .sidebar {
      background: var(--black); color: var(--white);
      padding: 0; position: fixed; top: 0; left: 0;
      width: 240px; height: 100vh; overflow-y: auto;
      display: flex; flex-direction: column;
    }
    .sidebar-logo {
      padding: 28px 28px 24px;
      font-family: var(--font-en); font-size: 22px; font-weight: 600;
      letter-spacing: 5px; border-bottom: 1px solid rgba(255,255,255,0.1);
      text-decoration: none; color: var(--white); display: block;
    }
    .sidebar-logo span { display: block; font-size: 10px; letter-spacing: 2px; color: var(--gold); margin-top: 4px; font-family: var(--font-kr); font-weight: 300; }

    .sidebar-user { padding: 20px 28px; border-bottom: 1px solid rgba(255,255,255,0.1); }
    .user-avatar { width: 44px; height: 44px; background: var(--gold); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 18px; margin-bottom: 10px; }
    .user-name { font-size: 14px; font-weight: 500; margin-bottom: 2px; }
    .user-status { font-size: 11px; letter-spacing: 1px; }
    .status-badge { display: inline-block; padding: 2px 8px; border-radius: 10px; font-size: 10px; letter-spacing: 1px; }
    .status-active { background: rgba(39,174,96,0.2); color: #27ae60; }
    .status-pending { background: rgba(201,169,110,0.2); color: var(--gold); }

    .sidebar-nav { padding: 16px 0; flex: 1; }
    .nav-section-title { font-size: 10px; letter-spacing: 3px; text-transform: uppercase; color: rgba(255,255,255,0.3); padding: 12px 28px 6px; }
    .nav-item {
      display: flex; align-items: center; gap: 12px;
      padding: 12px 28px; font-size: 13px; color: rgba(255,255,255,0.6);
      cursor: pointer; transition: all 0.2s; text-decoration: none;
      border-left: 3px solid transparent;
    }
    .nav-item:hover { color: var(--white); background: rgba(255,255,255,0.05); }
    .nav-item.active { color: var(--white); background: rgba(255,255,255,0.08); border-left-color: var(--gold); }
    .nav-icon { font-size: 16px; width: 20px; text-align: center; }

    .sidebar-bottom { padding: 20px 28px; border-top: 1px solid rgba(255,255,255,0.1); }
    .logout-btn { font-size: 12px; color: rgba(255,255,255,0.4); cursor: pointer; letter-spacing: 1px; background: none; border: none; font-family: var(--font-kr); }
    .logout-btn:hover { color: var(--white); }

    /* MAIN */
    .main-content { margin-left: 240px; padding: 40px; }

    .page-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 32px; }
    .page-title { font-family: var(--font-en); font-size: 32px; font-weight: 300; letter-spacing: 4px; }
    .page-date { font-size: 12px; color: var(--gray-dark); letter-spacing: 1px; }

    /* STATS */
    .stats-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-bottom: 32px; }
    .stat-card { background: var(--white); padding: 24px; border-top: 3px solid transparent; }
    .stat-card.gold { border-top-color: var(--gold); }
    .stat-card.black { border-top-color: var(--black); }
    .stat-card.green { border-top-color: #27ae60; }
    .stat-card.blue { border-top-color: #3498db; }
    .stat-label { font-size: 11px; letter-spacing: 2px; text-transform: uppercase; color: var(--gray-dark); margin-bottom: 12px; }
    .stat-value { font-family: var(--font-en); font-size: 32px; font-weight: 300; margin-bottom: 6px; }
    .stat-change { font-size: 12px; color: var(--gray-dark); }
    .stat-change.up { color: #27ae60; }
    .stat-change.down { color: #e74c3c; }

    /* REFERRAL LINK BOX */
    .ref-box { background: var(--black); color: var(--white); padding: 28px 32px; margin-bottom: 24px; display: flex; align-items: center; justify-content: space-between; gap: 24px; }
    .ref-box-left .ref-title { font-family: var(--font-en); font-size: 18px; font-weight: 300; letter-spacing: 3px; color: var(--gold); margin-bottom: 6px; }
    .ref-box-left .ref-sub { font-size: 12px; color: rgba(255,255,255,0.5); letter-spacing: 1px; }
    .ref-link-wrap { display: flex; gap: 0; flex: 1; max-width: 500px; }
    .ref-link-input { flex: 1; padding: 14px 16px; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.15); color: var(--white); font-size: 13px; font-family: var(--font-kr); outline: none; letter-spacing: 0.5px; }
    .ref-copy-btn { padding: 14px 20px; background: var(--gold); color: var(--black); border: none; font-size: 12px; letter-spacing: 2px; cursor: pointer; font-family: var(--font-kr); white-space: nowrap; transition: opacity 0.2s; }
    .ref-copy-btn:hover { opacity: 0.85; }

    /* GRID 2COL */
    .grid-2col { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 24px; }
    .grid-3col { display: grid; grid-template-columns: 2fr 1fr; gap: 20px; margin-bottom: 24px; }

    /* CARD */
    .card { background: var(--white); padding: 28px; }
    .card-title { font-size: 13px; letter-spacing: 2px; text-transform: uppercase; color: var(--black); margin-bottom: 20px; padding-bottom: 14px; border-bottom: 1px solid var(--gray-mid); display: flex; justify-content: space-between; align-items: center; }
    .card-title a { font-size: 11px; color: var(--gray-dark); text-decoration: none; letter-spacing: 1px; }
    .card-title a:hover { color: var(--black); }

    /* CHART */
    .chart-wrap { height: 180px; display: flex; align-items: flex-end; gap: 8px; padding: 0 4px; }
    .chart-bar-wrap { flex: 1; display: flex; flex-direction: column; align-items: center; gap: 6px; }
    .chart-bar { width: 100%; background: var(--gray-light); border-radius: 2px 2px 0 0; position: relative; transition: background 0.2s; cursor: pointer; }
    .chart-bar:hover { background: var(--gold); }
    .chart-bar.active { background: var(--black); }
    .chart-label { font-size: 10px; color: var(--gray-dark); letter-spacing: 0.5px; }
    .chart-val { font-size: 10px; color: var(--gray-dark); }

    /* SALES TABLE */
    .sales-table { width: 100%; border-collapse: collapse; }
    .sales-table th { font-size: 10px; letter-spacing: 2px; text-transform: uppercase; color: var(--gray-dark); padding: 10px 12px; text-align: left; border-bottom: 1px solid var(--gray-mid); }
    .sales-table td { padding: 14px 12px; font-size: 13px; border-bottom: 1px solid var(--gray-light); }
    .sales-table tr:last-child td { border-bottom: none; }
    .sales-table tr:hover td { background: var(--gray-light); }
    .order-status { display: inline-block; padding: 3px 10px; border-radius: 10px; font-size: 10px; letter-spacing: 1px; }
    .status-complete { background: rgba(39,174,96,0.1); color: #27ae60; }
    .status-pending { background: rgba(243,156,18,0.1); color: #f39c12; }
    .status-cancel { background: rgba(231,76,60,0.1); color: #e74c3c; }

    /* COMMISSION BREAKDOWN */
    .commission-breakdown { display: flex; flex-direction: column; gap: 16px; }
    .comm-item { display: flex; justify-content: space-between; align-items: center; }
    .comm-label { font-size: 13px; color: var(--gray-dark); }
    .comm-val { font-family: var(--font-en); font-size: 18px; font-weight: 300; }
    .comm-val.gold { color: var(--gold); }
    .comm-bar-wrap { margin-top: 4px; }
    .comm-bar { height: 4px; background: var(--gray-mid); border-radius: 2px; overflow: hidden; }
    .comm-fill { height: 100%; background: var(--gold); border-radius: 2px; }

    /* WITHDRAW */
    .withdraw-box { background: var(--gray-light); padding: 20px; margin-top: 16px; }
    .withdraw-amount { font-family: var(--font-en); font-size: 28px; font-weight: 300; color: var(--gold); margin-bottom: 4px; }
    .withdraw-label { font-size: 11px; color: var(--gray-dark); letter-spacing: 1px; margin-bottom: 16px; }
    .btn-withdraw { width: 100%; padding: 13px; background: var(--black); color: var(--white); border: none; font-size: 12px; letter-spacing: 2px; text-transform: uppercase; cursor: pointer; font-family: var(--font-kr); transition: opacity 0.2s; }
    .btn-withdraw:hover { opacity: 0.8; }

    /* SHARE LINKS */
    .share-links { display: flex; flex-direction: column; gap: 10px; }
    .share-item { display: flex; align-items: center; justify-content: space-between; padding: 12px 16px; background: var(--gray-light); }
    .share-platform { display: flex; align-items: center; gap: 10px; font-size: 13px; }
    .share-btn { padding: 6px 16px; background: var(--black); color: var(--white); border: none; font-size: 11px; letter-spacing: 1px; cursor: pointer; font-family: var(--font-kr); }

    /* TOAST */
    .toast { position: fixed; bottom: 32px; left: 50%; transform: translateX(-50%) translateY(80px); background: var(--black); color: var(--white); padding: 14px 32px; font-size: 13px; z-index: 9999; transition: transform 0.3s; pointer-events: none; }
    .toast.show { transform: translateX(-50%) translateY(0); }

    @media (max-width: 1024px) {
      .stats-grid { grid-template-columns: repeat(2, 1fr); }
      .grid-2col, .grid-3col { grid-template-columns: 1fr; }
    }
    @media (max-width: 768px) {
      .dashboard-wrap { grid-template-columns: 1fr; }
      .sidebar { position: static; width: 100%; height: auto; }
      .main-content { margin-left: 0; padding: 24px; }
      .stats-grid { grid-template-columns: repeat(2, 1fr); }
      .ref-box { flex-direction: column; align-items: flex-start; }
    }
  </style>
</head>
<body>

<div class="dashboard-wrap">
  <!-- SIDEBAR -->
  <aside class="sidebar">
    <a href="index.html" class="sidebar-logo">LEEL <span>Seller Dashboard</span></a>

    <div class="sidebar-user">
      <div class="user-avatar">👤</div>
      <p class="user-name" id="sidebarName">셀러</p>
      <p class="user-status">
        <span class="status-badge status-active" id="sidebarStatus">● 활성</span>
      </p>
    </div>

    <nav class="sidebar-nav">
      <p class="nav-section-title">Main</p>
      <a class="nav-item active" onclick="showSection('dashboard')">
        <span class="nav-icon">📊</span> 대시보드
      </a>
      <a class="nav-item" onclick="showSection('mylink')">
        <span class="nav-icon">🔗</span> 내 링크 관리
      </a>
      <a class="nav-item" onclick="showSection('sales')">
        <span class="nav-icon">🛍️</span> 판매 내역
      </a>
      <a class="nav-item" onclick="showSection('commission')">
        <span class="nav-icon">💰</span> 수수료 내역
      </a>
      <p class="nav-section-title">Account</p>
      <a class="nav-item" onclick="showSection('profile')">
        <span class="nav-icon">👤</span> 프로필 설정
      </a>
      <a class="nav-item" href="index.html">
        <span class="nav-icon">🏠</span> 쇼핑몰 바로가기
      </a>
    </nav>

    <div class="sidebar-bottom">
      <button class="logout-btn" onclick="logout()">로그아웃 →</button>
    </div>
  </aside>

  <!-- MAIN -->
  <main class="main-content">

    <!-- ── 대시보드 섹션 ── -->
    <div id="section-dashboard">
      <div class="page-header">
        <h1 class="page-title">Dashboard</h1>
        <span class="page-date" id="todayDate"></span>
      </div>

      <!-- STATS -->
      <div class="stats-grid">
        <div class="stat-card gold">
          <p class="stat-label">총 판매금액</p>
          <p class="stat-value" id="statTotalSales">₩0</p>
          <p class="stat-change up">↑ 이번 달</p>
        </div>
        <div class="stat-card black">
          <p class="stat-label">총 수수료</p>
          <p class="stat-value" id="statTotalComm">₩0</p>
          <p class="stat-change up">↑ 20% 적용</p>
        </div>
        <div class="stat-card green">
          <p class="stat-label">총 주문 수</p>
          <p class="stat-value" id="statOrders">0건</p>
          <p class="stat-change">누적</p>
        </div>
        <div class="stat-card blue">
          <p class="stat-label">링크 클릭 수</p>
          <p class="stat-value" id="statClicks">0</p>
          <p class="stat-change">누적</p>
        </div>
      </div>

      <!-- 레퍼럴 링크 박스 -->
      <div class="ref-box">
        <div class="ref-box-left">
          <p class="ref-title">✦ 나의 셀러 링크</p>
          <p class="ref-sub">이 링크를 공유하면 구매 시 20% 수수료를 받습니다</p>
        </div>
        <div class="ref-link-wrap">
          <input class="ref-link-input" type="text" id="refLinkInput" readonly/>
          <button class="ref-copy-btn" onclick="copyLink()">링크 복사</button>
        </div>
      </div>

      <!-- 차트 + 수수료 -->
      <div class="grid-3col">
        <div class="card">
          <p class="card-title">월별 판매 현황 <a href="#">자세히 보기</a></p>
          <div class="chart-wrap" id="chartWrap"></div>
        </div>
        <div class="card">
          <p class="card-title">수수료 현황</p>
          <div class="commission-breakdown">
            <div class="comm-item">
              <span class="comm-label">총 판매금액</span>
              <span class="comm-val" id="commTotal">₩0</span>
            </div>
            <div class="comm-item">
              <span class="comm-label">수수료율</span>
              <span class="comm-val">20%</span>
            </div>
            <div class="comm-bar-wrap">
              <div class="comm-bar"><div class="comm-fill" style="width:20%"></div></div>
            </div>
            <div class="comm-item">
              <span class="comm-label">획득 수수료</span>
              <span class="comm-val gold" id="commEarned">₩0</span>
            </div>
            <div class="comm-item">
              <span class="comm-label">정산 완료</span>
              <span class="comm-val" id="commPaid">₩0</span>
            </div>
          </div>
          <div class="withdraw-box">
            <p class="withdraw-amount" id="withdrawAmt">₩0</p>
            <p class="withdraw-label">출금 가능 금액</p>
            <button class="btn-withdraw" onclick="requestWithdraw()">출금 신청</button>
          </div>
        </div>
      </div>

      <!-- 최근 판매 -->
      <div class="card" style="margin-bottom:24px;">
        <p class="card-title">최근 판매 내역 <a href="#">전체 보기</a></p>
        <table class="sales-table">
          <thead>
            <tr>
              <th>주문번호</th>
              <th>상품명</th>
              <th>판매금액</th>
              <th>수수료(20%)</th>
              <th>상태</th>
              <th>날짜</th>
            </tr>
          </thead>
          <tbody id="recentSalesBody"></tbody>
        </table>
      </div>

      <!-- SNS 공유 -->
      <div class="card">
        <p class="card-title">SNS 공유하기</p>
        <div class="share-links">
          <div class="share-item">
            <div class="share-platform"><span>📸</span> 인스타그램</div>
            <button class="share-btn" onclick="shareLink('instagram')">링크 복사</button>
          </div>
          <div class="share-item">
            <div class="share-platform"><span>💛</span> 카카오톡</div>
            <button class="share-btn" onclick="shareLink('kakao')">공유하기</button>
          </div>
          <div class="share-item">
            <div class="share-platform"><span>🐦</span> 트위터/X</div>
            <button class="share-btn" onclick="shareLink('twitter')">공유하기</button>
          </div>
          <div class="share-item">
            <div class="share-platform"><span>📘</span> 페이스북</div>
            <button class="share-btn" onclick="shareLink('facebook')">공유하기</button>
          </div>
        </div>
      </div>
    </div>

    <!-- ── 내 링크 관리 섹션 ── -->
    <div id="section-mylink" style="display:none;">
      <div class="page-header">
        <h1 class="page-title">My Links</h1>
      </div>
      <div class="card">
        <p class="card-title">링크 생성 & 관리</p>
        <div style="margin-bottom:24px;">
          <p style="font-size:13px;color:var(--gray-dark);line-height:2;margin-bottom:16px;">
            아래 링크를 SNS, 블로그, 유튜브 등에 공유하세요.<br/>
            링크를 통해 구매가 발생하면 판매금액의 <strong style="color:var(--black);">20%</strong>가 수수료로 지급됩니다.
          </p>
          <div style="background:var(--gray-light);padding:20px;margin-bottom:16px;">
            <p style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--gray-dark);margin-bottom:8px;">메인 링크</p>
            <div style="display:flex;gap:0;">
              <input class="ref-link-input" type="text" id="refLinkInput2" readonly style="flex:1;padding:14px 16px;border:1px solid var(--gray-mid);font-size:13px;outline:none;background:var(--white);"/>
              <button class="ref-copy-btn" onclick="copyLink2()" style="padding:14px 20px;background:var(--black);color:var(--white);border:none;font-size:12px;letter-spacing:2px;cursor:pointer;font-family:var(--font-kr);">복사</button>
            </div>
          </div>

          <p style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--gray-dark);margin-bottom:12px;">상품별 링크</p>
          <div id="productLinks"></div>
        </div>
      </div>
    </div>

    <!-- ── 판매 내역 섹션 ── -->
    <div id="section-sales" style="display:none;">
      <div class="page-header">
        <h1 class="page-title">Sales History</h1>
      </div>
      <div class="card">
        <p class="card-title">전체 판매 내역</p>
        <table class="sales-table">
          <thead>
            <tr>
              <th>주문번호</th><th>상품명</th><th>수량</th>
              <th>판매금액</th><th>수수료(20%)</th><th>상태</th><th>날짜</th>
            </tr>
          </thead>
          <tbody id="allSalesBody"></tbody>
        </table>
      </div>
    </div>

    <!-- ── 수수료 내역 섹션 ── -->
    <div id="section-commission" style="display:none;">
      <div class="page-header">
        <h1 class="page-title">Commission</h1>
      </div>
      <div class="stats-grid" style="margin-bottom:24px;">
        <div class="stat-card gold">
          <p class="stat-label">총 수수료</p>
          <p class="stat-value" id="commStatTotal">₩0</p>
        </div>
        <div class="stat-card green">
          <p class="stat-label">정산 완료</p>
          <p class="stat-value" id="commStatPaid">₩0</p>
        </div>
        <div class="stat-card black">
          <p class="stat-label">정산 대기</p>
          <p class="stat-value" id="commStatPending">₩0</p>
        </div>
        <div class="stat-card blue">
          <p class="stat-label">출금 가능</p>
          <p class="stat-value" id="commStatAvail">₩0</p>
        </div>
      </div>
      <div class="card">
        <p class="card-title">수수료 내역</p>
        <table class="sales-table">
          <thead>
            <tr><th>날짜</th><th>내용</th><th>판매금액</th><th>수수료</th><th>상태</th></tr>
          </thead>
          <tbody id="commBody"></tbody>
        </table>
      </div>
    </div>

    <!-- ── 프로필 섹션 ── -->
    <div id="section-profile" style="display:none;">
      <div class="page-header">
        <h1 class="page-title">Profile</h1>
      </div>
      <div class="card">
        <p class="card-title">프로필 설정</p>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
          <div class="form-group" style="display:flex;flex-direction:column;gap:8px;">
            <label style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--gray-dark);">이름</label>
            <input class="form-input" type="text" id="profileName" style="padding:14px 16px;border:1px solid var(--gray-mid);font-size:14px;font-family:var(--font-kr);outline:none;"/>
          </div>
          <div class="form-group" style="display:flex;flex-direction:column;gap:8px;">
            <label style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--gray-dark);">이메일</label>
            <input class="form-input" type="email" id="profileEmail" style="padding:14px 16px;border:1px solid var(--gray-mid);font-size:14px;font-family:var(--font-kr);outline:none;" readonly/>
          </div>
          <div class="form-group" style="display:flex;flex-direction:column;gap:8px;">
            <label style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--gray-dark);">연락처</label>
            <input class="form-input" type="tel" id="profilePhone" style="padding:14px 16px;border:1px solid var(--gray-mid);font-size:14px;font-family:var(--font-kr);outline:none;"/>
          </div>
          <div class="form-group" style="display:flex;flex-direction:column;gap:8px;">
            <label style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--gray-dark);">채널 URL</label>
            <input class="form-input" type="text" id="profileChannel" style="padding:14px 16px;border:1px solid var(--gray-mid);font-size:14px;font-family:var(--font-kr);outline:none;"/>
          </div>
          <div class="form-group" style="display:flex;flex-direction:column;gap:8px;">
            <label style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--gray-dark);">정산 계좌은행</label>
            <select style="padding:14px 16px;border:1px solid var(--gray-mid);font-size:14px;font-family:var(--font-kr);outline:none;background:var(--white);">
              <option>국민은행</option><option>신한은행</option><option>우리은행</option>
              <option>하나은행</option><option>카카오뱅크</option><option>토스뱅크</option>
            </select>
          </div>
          <div class="form-group" style="display:flex;flex-direction:column;gap:8px;">
            <label style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--gray-dark);">계좌번호</label>
            <input style="padding:14px 16px;border:1px solid var(--gray-mid);font-size:14px;font-family:var(--font-kr);outline:none;" type="text" placeholder="계좌번호 입력"/>
          </div>
        </div>
        <button onclick="saveProfile()" style="margin-top:24px;padding:14px 40px;background:var(--black);color:var(--white);border:none;font-size:12px;letter-spacing:3px;text-transform:uppercase;cursor:pointer;font-family:var(--font-kr);">저장하기</button>
      </div>
    </div>

  </main>
</div>

<div class="toast" id="toast"></div>

<script>
  const user = JSON.parse(localStorage.getItem('leel_user') || '{}');
  const orders = JSON.parse(localStorage.getItem('leel_orders') || '[]');

  // 데모 데이터
  const demoSales = [
    { id:'LEEL-001', product:'Minimal Linen Dress', qty:1, amount:89000, commission:17800, status:'complete', date:'2025.06.20' },
    { id:'LEEL-002', product:'Silk Wrap Blouse', qty:2, amount:130000, commission:26000, status:'complete', date:'2025.06.18' },
    { id:'LEEL-003', product:'Wide Leg Trousers', qty:1, amount:72000, commission:14400, status:'pending', date:'2025.06.15' },
    { id:'LEEL-004', product:'Pleated Midi Skirt', qty:1, amount:68000, commission:13600, status:'complete', date:'2025.06.10' },
    { id:'LEEL-005', product:'Oversized Blazer', qty:1, amount:128000, commission:25600, status:'cancel', date:'2025.06.05' },
  ];

  const chartData = [
    { month:'1월', val:120000 }, { month:'2월', val:180000 },
    { month:'3월', val:150000 }, { month:'4월', val:220000 },
    { month:'5월', val:280000 }, { month:'6월', val:350000 },
  ];

  function init() {
    // 유저 정보
    document.getElementById('sidebarName').textContent = user.name || '셀러';
    document.getElementById('todayDate').textContent = new Date().toLocaleDateString('ko-KR', { year:'numeric', month:'long', day:'numeric' });

    // 셀러 ID & 링크
    const sellerId = user.sellerId || 'seller_demo';
    const refLink = `${window.location.origin}/index.html?ref=${sellerId}`;
    document.getElementById('refLinkInput').value = refLink;
    if (document.getElementById('refLinkInput2')) document.getElementById('refLinkInput2').value = refLink;

    // 통계 계산
    const myOrders = orders.filter(o => o.ref === sellerId);
    const totalSales = demoSales.filter(s => s.status !== 'cancel').reduce((s,i) => s + i.amount, 0) + myOrders.reduce((s,i) => s + i.total, 0);
    const totalComm = Math.floor(totalSales * 0.2);
    const totalOrders = demoSales.length + myOrders.length;

    document.getElementById('statTotalSales').textContent = `₩${totalSales.toLocaleString()}`;
    document.getElementById('statTotalComm').textContent = `₩${totalComm.toLocaleString()}`;
    document.getElementById('statOrders').textContent = `${totalOrders}건`;
    document.getElementById('statClicks').textContent = Math.floor(Math.random() * 500 + 100);
    document.getElementById('commTotal').textContent = `₩${totalSales.toLocaleString()}`;
    document.getElementById('commEarned').textContent = `₩${totalComm.toLocaleString()}`;
    document.getElementById('commPaid').textContent = `₩${Math.floor(totalComm * 0.6).toLocaleString()}`;
    document.getElementById('withdrawAmt').textContent = `₩${Math.floor(totalComm * 0.4).toLocaleString()}`;

    // 수수료 섹션
    document.getElementById('commStatTotal').textContent = `₩${totalComm.toLocaleString()}`;
    document.getElementById('commStatPaid').textContent = `₩${Math.floor(totalComm*0.6).toLocaleString()}`;
    document.getElementById('commStatPending').textContent = `₩${Math.floor(totalComm*0.2).toLocaleString()}`;
    document.getElementById('commStatAvail').textContent = `₩${Math.floor(totalComm*0.4).toLocaleString()}`;

    // 프로필
    if (document.getElementById('profileName')) {
      document.getElementById('profileName').value = user.name || '';
      document.getElementById('profileEmail').value = user.email || '';
      document.getElementById('profilePhone').value = user.phone || '';
      document.getElementById('profileChannel').value = user.channelUrl || '';
    }

    renderChart();
    renderRecentSales();
    renderAllSales();
    renderCommission();
    renderProductLinks(sellerId);
  }

  function renderChart() {
    const max = Math.max(...chartData.map(d => d.val));
    document.getElementById('chartWrap').innerHTML = chartData.map((d, i) => `
      <div class="chart-bar-wrap">
        <div class="chart-val">₩${(d.val/10000).toFixed(0)}만</div>
        <div class="chart-bar \${i === chartData.length-1 ? 'active' : ''}"
          style="height:${Math.floor((d.val/max)*160)}px;"
          title="${d.month}: ₩${d.val.toLocaleString()}"></div>
        <div class="chart-label">${d.month}</div>
      </div>
    `).join('');
  }

  function renderRecentSales() {
    document.getElementById('recentSalesBody').innerHTML = demoSales.slice(0,4).map(s => `
      <tr>
        <td style="font-family:var(--font-en);font-size:12px;">${s.id}</td>
        <td>${s.product}</td>
        <td>₩${s.amount.toLocaleString()}</td>
        <td style="color:var(--gold);font-family:var(--font-en);">₩${s.commission.toLocaleString()}</td>
        <td><span class="order-status status-${s.status}">${s.status==='complete'?'완료':s.status==='pending'?'대기':'취소'}</span></td>
        <td style="font-size:12px;color:var(--gray-dark);">${s.date}</td>
      </tr>
    `).join('');
  }

  function renderAllSales() {
    if (!document.getElementById('allSalesBody')) return;
    document.getElementById('allSalesBody').innerHTML = demoSales.map(s => `
      <tr>
        <td style="font-family:var(--font-en);font-size:12px;">${s.id}</td>
        <td>${s.product}</td>
        <td>${s.qty}</td>
        <td>₩${s.amount.toLocaleString()}</td>
        <td style="color:var(--gold);">₩${s.commission.toLocaleString()}</td>
        <td><span class="order-status status-${s.status}">${s.status==='complete'?'완료':s.status==='pending'?'대기':'취소'}</span></td>
        <td style="font-size:12px;color:var(--gray-dark);">${s.date}</td>
      </tr>
    `).join('');
  }

  function renderCommission() {
    if (!document.getElementById('commBody')) return;
    document.getElementById('commBody').innerHTML = demoSales.map(s => `
      <tr>
        <td style="font-size:12px;color:var(--gray-dark);">${s.date}</td>
        <td>${s.product} 판매</td>
        <td>₩${s.amount.toLocaleString()}</td>
        <td style="color:var(--gold);font-family:var(--font-en);">₩${s.commission.toLocaleString()}</td>
        <td><span class="order-status status-${s.status}">${s.status==='complete'?'정산완료':s.status==='pending'?'정산대기':'취소'}</span></td>
      </tr>
    `).join('');
  }

  function renderProductLinks(sellerId) {
    if (!document.getElementById('productLinks')) return;
    const products = [
      { id:1, name:'Minimal Linen Dress', price:'₩89,000' },
      { id:2, name:'Silk Wrap Blouse', price:'₩65,000' },
      { id:3, name:'Wide Leg Trousers', price:'₩72,000' },
      { id:4, name:'Cotton Knit Cardigan', price:'₩58,000' },
    ];
    document.getElementById('productLinks').innerHTML = products.map(p => `
      <div style="display:flex;align-items:center;justify-content:space-between;padding:14px 16px;background:var(--gray-light);margin-bottom:8px;">
        <div>
          <p style="font-size:13px;font-weight:500;margin-bottom:2px;">${p.name}</p>
          <p style="font-size:12px;color:var(--gray-dark);">${p.price} · 수수료 20%</p>
        </div>
        <button onclick="copyProductLink(${p.id}, '${sellerId}')"
          style="padding:8px 16px;background:var(--black);color:var(--white);border:none;font-size:11px;letter-spacing:1px;cursor:pointer;font-family:var(--font-kr);">
          링크 복사
        </button>
      </div>
    `).join('');
  }

  function copyLink() {
    const val = document.getElementById('refLinkInput').value;
    navigator.clipboard.writeText(val).then(() => showToast('링크가 복사되었습니다!'));
  }

  function copyLink2() {
    const val = document.getElementById('refLinkInput2').value;
    navigator.clipboard.writeText(val).then(() => showToast('링크가 복사되었습니다!'));
  }

  function copyProductLink(productId, sellerId) {
    const link = `${window.location.origin}/product.html?id=${productId}&ref=${sellerId}`;
    navigator.clipboard.writeText(link).then(() => showToast('상품 링크가 복사되었습니다!'));
  }

  function shareLink(platform) {
    const link = document.getElementById('refLinkInput').value;
    const urls = {
      twitter: `https://twitter.com/intent/tweet?text=LEEL 쇼핑몰에서 예쁜 옷 구경하세요!&url=${encodeURIComponent(link)}`,
      facebook: `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(link)}`,
    };
    if (urls[platform]) window.open(urls[platform], '_blank');
    else { navigator.clipboard.writeText(link); showToast(`${platform} 링크가 복사되었습니다!`); }
  }

  function showSection(name) {
    document.querySelectorAll('[id^="section-"]').forEach(s => s.style.display = 'none');
    document.getElementById(`section-${name}`).style.display = 'block';
    document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
    event.currentTarget.classList.add('active');
  }

  function requestWithdraw() {
    showToast('출금 신청이 완료되었습니다. 3-5 영업일 내 처리됩니다.');
  }

  function saveProfile() {
    showToast('프로필이 저장되었습니다');
  }

  function logout() {
    localStorage.removeItem('leel_user');
    location.href = 'login.html';
  }

  function showToast(msg) {
    const t = document.getElementById('toast');
    t.textContent = msg;
    t.classList.add('show');
    setTimeout(() => t.classList.remove('show'), 2800);
  }

  init();
</script>
</body>
</html>
